{
  "name": "umbraco-commerce-relewise-integration",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --env development",
    "start": "wrangler dev --env development"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.1.0",
    "vitest": "1.3.0",
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "@relewise/client": "^1.25.0",
    "@relewise/integrations": "^1.0.1"
  }
}
